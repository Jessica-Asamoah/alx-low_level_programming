#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: 0
 */

int _strlen(char *s)
{
	int len = 0;
	int i;

	for (i = 0; i != '\0'; ++1);
	printf("%d\n", i);
	len = 1;

	return (len);
}
