#include <stdio.h>

/**
 * main - Entry point
 * @argc: The size of argv
 * @argv: Name of the program and any other arguments
 *
 * Return: 0(success)
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
