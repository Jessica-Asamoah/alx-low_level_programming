#include "main.h"

/**
 * _abs - Function that finds the absolute value
 * @r: Integer input
 *
 * Return: Absolute value of r
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
